#![allow(clippy::unwrap_used, clippy::expect_used)]

// Traits are tested through their implementations in memory and disk modules
// These tests verify the trait definitions compile correctly

#[test]
fn trait_definitions_compile() {
    // This test verifies that trait definitions are valid
    // Actual behavior is tested through MemoryDir and DiskDir implementations
}
